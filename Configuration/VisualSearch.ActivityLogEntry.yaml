Searches:
  # ===========================================================================
  # Suche nach Prüfungsort
  # Diese Suche ist der Hauptfilter zum Filtern von Prüfungsorten
  # ===========================================================================
  fucodo\contact\securitycenter\Domain\Model\ActivityLogEntry:
    search:
      repository: fucodo\contact\securitycenter\Domain\Repository\ActivityLogEntryRepository
    sorting:
      1100:
        label: Datum aufsteigend
        fields:
          'createdAt': ASC
      1200:
        label: Datum fallend
        fields:
          'createdAt': DESC
    autocomplete:
      searchtext:
        label: "Suchtext"
        selector:
          conditions:
            once: true
          freeInput: true
        matches:
          like:
            - title
            - userIdentity
            - message
            - code
      date:
        label: "Datum (Tag)"
        inputType: date
        selector:
          conditions:
            once: true
          freeInput: true
          dateFormat: Y-m-d
        matches:
          \KayStrobach\VisualSearch\Demands\Date\DateDemand:
            - createdAt
      code:
        label: "Code"
        selector:
          conditions:
            once: true
          freeInput: true
        matches:
          like:
            - code
      severity:
        label: "Auswirkung"
        selector:
          conditions:
            once: true
          values:
            Notice: Notice
            Warning: Warning
            Error: Error
            OK: OK
        matches:
          like:
            - severity
        # fails due to restricons in the querybuilder
        # system:
        #   label: "Systeminformationen"
        #   selector:
        #     conditions:
        #       once: true
        #     freeInput: true
        #   matches:
        #     like:
        #       - device.clientFamily
        #       - device.clientVersion
        #       - device.clientEngine
        #       - device.osFamily
        #       - device.isInfo
        #       - device.osVersion
        #       - device.brandName
        #       - device.model
